Make sure of the training_dataset and test_dataset Directories;
Then run generate_surf_data.m;
then run nn4monu.m, which will be using the Workspace variables initialised by running above file; Done !